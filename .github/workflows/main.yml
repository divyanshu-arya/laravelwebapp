name: Deploy to Amazon ECS

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    environment: production

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Testing to create a file
      run: |
        ssh -i ${{secret.MUMBAI_KEY}} ec2-user@ec2-3-110-103-159.ap-south-1.compute.amazonaws.com
        echo "File created" >> ~/File.txt
